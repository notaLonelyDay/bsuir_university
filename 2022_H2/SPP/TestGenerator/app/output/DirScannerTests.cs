using dir_scanner;
using dir_scanner.entity;
using dir_scanner.util;
using FluentAssertions;
using Xunit;
using Xunit.Abstractions;
using Xunit;

public class ScannerTestsTest
{
    [Fact]
    public void ShouldWorkIfValidTest()
    {
        Assert.True(false);
    }

    [Fact]
    public void ShouldThrowIfPathNotValidTest()
    {
        Assert.True(false);
    }

    [Fact]
    public void ShouldThrowIfThreadsNotValidTest()
    {
        Assert.True(false);
    }
}